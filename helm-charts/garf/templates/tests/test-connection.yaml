apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "garf.fullname" . }}-test-connection"
  labels:
    {{- include "garf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "garf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
